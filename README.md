# μsh | Micro Shell | UADE Shell
Una implementación ultra minimalista de un interprete de mandatos para sistemas basados en POSIX.

## LINUX
Puede compilarse con `gcc main.c -o ush` y ejecutarse con `./ush`
